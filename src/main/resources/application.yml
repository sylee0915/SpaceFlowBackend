spring:
  # 1. H2 Database ??
  h2:
    console:
      enabled: true
      path: /h2-console # H2 ?? ?? ??

  # 2. JPA ??
  jpa:
    hibernate:
      ddl-auto: update # ?? ????? update? ?? (?? ??? none ?? validate)
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        format_sql: true # ?? ???

  # 3. ??? ?? (DB ??) ??
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL # ???? DB ?? (MySQL ??)
    username: sa
    password:

# 4. JWT Custom ?? (JwtProperties.java?? ??)
jwt:
  # ?? ??? ???? ?? ?. (base64 ???? ??? ??, ?? 32??? ??)
  # ? ?? ??? ??? ?????? ? ???.
  secret-key: aGVsbG93b3JsZGhhdGlzamV3dHNlY3JldGtlZm9ydGVzdGluZ2FwcGxpY2F0aW9uCg==

  # Access Token ?? ?? (???) - ?: 30?
  access-token-expiration-time: 1800000

  # Refresh Token ?? ?? (???) - ?: 7?
  refresh-token-expiration-time: 604800000

  # ?? ?? Prefix (?? ??)
  token-prefix: Bearer

  # ??? ?? HTTP ?? ??
  header-string: Authorization

server:
  port: 8080

logging:
  level:
    # ?? ?? ?? ?? ??
    org.hibernate.SQL: debug